play.i18n.langs = "en"

akka {
  loglevel = "DEBUG"
  actor {
    debug {
      receive = on
      autoreceive = on
      lifecycle = on
    }
  }

  extensions = ["com.rxthings.di.InjectExt"]
  inject.mode = "spi"
}
